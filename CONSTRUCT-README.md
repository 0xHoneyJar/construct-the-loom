<!-- Generated by .claude/scripts/butterfreezone-construct-gen.sh -->
<!-- Canonical source: construct.yaml · do not edit by hand -->

# the-loom

> Composition deck-builder

## About

| field | value |
|---|---|
| slug | `the-loom` |
| version | `0.1.0` |
| schema_version | `3` |
| author | 0xHoneyJar |
| license | MIT |

Spell-book × deck-builder for loa-compositions. Browse, compose, invoke. Persona: DEALER.

## Personas

- `@DEALER` → [`identity/DEALER.md`](identity/DEALER.md)

## Skills

- `playing-compositions` — Play loa-compositions via the `loom` CLI — the spell-book × deck-builder surface. Browse the 5-card hand (recents + pinned), open the full palette via fzf, invoke a composition with a live progress ribbon, inspect recipes (yaml), pin/unpin to home view. Trigger phrases include "fire <composition>", "what compositions exist", "run audit-feel", "show me the loom", "play <composition>", "pin this composition". Wraps `compose-run.sh` with operator-friendly semantics. NOT for authoring NEW compositions (use the-weaver/composing-paths) or validating syntax (use loa-compositions/scripts/validate.sh).

## Commands

- `/loom` — /loom — composition deck-builder (DEALER)

## Composes with

_None declared._

## Streams

**Reads**:

- Artifact
- Intent

**Writes**:

- Artifact
- Signal
- Verdict

## Grimoires read/write (SEED §12)

**Reads from**:

- `grimoires/{product}/.run/compose/`
- `grimoires/{product}/loa-compositions/compositions/`

**Writes to**:

- `.run/compose/{run_id}/orchestrator.jsonl`

> The grimoire path IS the interface — constructs writing to the same path compose automatically.

## Install

```bash
/constructs install the-loom
```

## Provenance

- Canonical spec: `construct.yaml`
- Generator: `.claude/scripts/butterfreezone-construct-gen.sh`
- Doctrine: `bonfire-construct-pipe-doctrine.md` §3 typed streams · §4 composition · §14.2 Operator-Model
